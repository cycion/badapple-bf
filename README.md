# Bad Apple in BF

## Requirements
- Python 3.6+, for modules see `requirements.txt`
- youtube-dl

## Run
```
chmod +x ./launch.sh
./launch.sh
```
Open the files in `output\` with a text editor to see the results. The generated BF code is in `bfcode` and each animation frame is in `text`

## License
See LICENSE
